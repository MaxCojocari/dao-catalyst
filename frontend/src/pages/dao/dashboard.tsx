import { DaoOverview } from "../../components";

export const DashboardPage = () => {
  // const hasProposals = proposals.length > 0;
  // const hasTransfers = transfers.length > 0;

  return (
    <>
      <DaoOverview />
    </>
  );
};
