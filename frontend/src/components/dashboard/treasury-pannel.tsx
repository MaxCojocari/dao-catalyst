import { Container } from "../common-styles";

export const TreasuryPanel = ({
  transfers,
  balance,
}: {
  transfers: any[];
  balance: string;
}) => {
  return <Container>Treasury pannel</Container>;
};
