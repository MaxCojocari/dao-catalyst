export type StatusType = "success" | "pending" | "error" | "active";

export enum StatusTimelineType {
  Published = "Published",
  Running = "Running",
  Succeeded = "Succeeded",
  Executed = "Executed",
  Rejected = "Rejected",
}
